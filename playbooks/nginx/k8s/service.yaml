apiVersion: v1
kind: Service
metadata:
  name: nginx-static
  namespace: web
spec:
  type: NodePort
  selector:
    app: nginx-static
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080

